"use client";

// The cockpit is one page. This is it.
//
// Stub: the direct-manipulation canvas (drag to arrange, close a tile, add
// from the tray) replaces this. Kept minimal so routing and the shell can be
// wired against it in parallel.

export function CanvasView() {
  return null;
}
